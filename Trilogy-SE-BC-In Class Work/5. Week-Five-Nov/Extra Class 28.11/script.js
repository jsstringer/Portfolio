Prepare Global Variables
var score = 0
var questions = [{
    question: "",
    choices:
}


var totalSeconds =90
var remainingSeconds = totalSeconds;

// start button onclick
// hide start button


// display the first question
    // get the first question. q1 = questions[0]
    render(q1)
    // set onclick for each choice question
        //when any choice is clicked
        // get userChoice from button (which did user choose?)
        // compare userChoice with answer 
        // if correct
        // timer --
        // render (q2)
        // alert user


function render(question) {
    //titleEL = doc.getTittle
    // choiceListEL =document.getChoice

    // question.getTitle
    // question.getChoices
    // question.getAnswers

    // titleEl.text = questionTitle
    // choicesList = 
}

function render (timer) {


}

function render (score) {
 

}


}