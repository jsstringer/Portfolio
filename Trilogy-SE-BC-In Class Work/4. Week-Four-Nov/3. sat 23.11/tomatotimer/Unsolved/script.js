var statusSpan = document.querySelector("#status");
var statusToggle = document.querySelector("#status-toggle");
var playButton = document.querySelector("#play");
var pauseButton = document.querySelector("#pause");
var stopButton = document.querySelector("#stop");
var minutesDisplay = document.querySelector("#minutes");
var secondsDisplay = document.querySelector("#seconds");
var workMinutesInput = document.querySelector("#work-minutes");
var restMinutesInput = document.querySelector("#rest-minutes");

var totalSeconds = 0;
var secondsElapsed = 0;
var interval;


************DOM - document********


localStorage
var person

varpersonString = JSON.stringify(person); // JSON to Sting
localStorage.setItem

var seconds = 0;

var interval;

function (minutes) {

  seconds = mins*60  25;

  interval = setInterval (function){
    seconds--;

    if(seconds) {

      clearInterval(interval);

      alert
    }
  }
}


                                              maths.floor
function startTimer() {
  // Write code to start the timer here
}

playButton.addEventListener("click", startTimer);
function.startTimer
startTimer
// event listern clicks the button to start the hand
// event listern click starts the seconds timer

pauseButton.addEventListener("click", pauseTimer);
function.stopTimer
stopTimer
//event listern clicks button to pause the hand
//event listern clicks button to pause the seconds timer


stopButton.addEventListener("click", stopTimer);
//event listern clicks stopbutton to rest the hand
//event listern clicks stopbutton to restet the seconds timer

sliderButton.addEventListener("click", statusToggle);
//change the mode from working to resting timer

function stopTimer (){
  seconsElaped = 0;
  setTimeout();
  rendertime();


}


rendertime